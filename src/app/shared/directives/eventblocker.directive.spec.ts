import { EventblockerDirective } from './eventblocker.directive';

describe('EventblockerDirective', () => {
  it('should create an instance', () => {
    const directive = new EventblockerDirective();
    expect(directive).toBeTruthy();
  });
});
